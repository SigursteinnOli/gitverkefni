1. Hvað gera eftirfarandi Linux skipanir?
	cd      = Change directory, skiptir um directory
	cd ~    = Change to home directory
	ls      = sýnir lista af öllum skjölum í current directory
	pwd     = listir öll directory í current directory
	mkdir   = býr til nýtt directory

2. Hvað gera eftirfarandi git skipanir og hvers vegna eru þær gagnlegar?
	git clone     = afritar repository inní nýtt directory
	git log       = sýnir commit log'inn
	git status    = sýnir slúðir frá index skjalinu til HEAD
	git diff      = Show the working tree status
	git checkout  = Switch branches or restore working tree files

3. Hver er munurinn á eftirfarandi git skipunum:
	a) git diff   = Show changes between the commits
	b) git diff --staged  = Will only show changes in the stages area
	c) git diff commit1 commit2 = cybertreffen

4. 	Hvað er útgáfustýring (e.version control)? 
 Version Control keeps track of all changes you do so you can call back an old verion of your git

5.	Hverjir eru helstu kostir við að nota GIT?
5. Hægt er að fara til baka og endur gera skref. Líka er mjög gott að geta unnið í hópum og öðru fólki gegnum git

6.	Hversu oft telur þú að eigi að gera commit í verkefni, rökstuddu?
 Fer bara algjörlega eftir verkefninu

7.	Hvað er "Working directory"?" "Staging area"?" og "Repository" í GIT?
 Working directory er directoryið sem þú ert að vinna í
 staging area is a step before you commit
 repository is a digital directory

8.	Hvenær er sniðugt að nota greinar (branches)? 
 að nota branch er einfaldara

9.	Hver fann upp GIT?
 Linus Torvalds

10.	Hvar er GIT helst notað?
 Um allan heim og í vinnusvæðum sem nota mikið af tölvum